package main

import (
    "fmt"
)

const p string = "death & taxes"

func main() {
    const q = 42
    const s = "string"
    fmt.Println("p -", p)
    fmt.Println("q -", q)
    fmt.Println("s -", s)
}
